# Database configuration
DB_HOST=127.0.0.1
DB_NAME=epss_v300
DB_USER=epss_user
DB_PASS=StrongPassword123!

# Application URL base (use "/" when deploying at domain root)
BASE_URL=/

# Enable verbose error output in development (set to 1)
APP_DEBUG=1
